#!/usr/bin/expect -f

spawn jdb -classpath ./target/classes/ -sourcepath ./src/main/java/pl.lodz.p.it.isrp.Start 5

expect ">"
send -- "stop on pl.lodz.p.it.isrp.SortTabNumbers:29\r"

expect ">"
send --"run\r"

send -- "print i\r"
send -- "print j\r"

expect eof